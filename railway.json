{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan db:seed --class=ShopSeeder && envsubst '$PORT' < /etc/nginx/sites-available/default > /tmp/nginx-default && cp /tmp/nginx-default /etc/nginx/sites-available/default && service nginx start && php-fpm",
    "healthcheckPath": "/up",
    "restartPolicyType": "ON_FAILURE"
  }
}
