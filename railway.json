{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan db:seed --class=ShopSeeder && apache2-foreground",
    "healthcheckPath": "/up",
    "restartPolicyType": "ON_FAILURE"
  }
}
